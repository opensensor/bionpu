schema_version: "1.0.0"
task_id: ""
# consolidated gaps live in
# `bionpu/kernels/basecalling/lstm_cell_bf16/gaps.yaml` (the
# aggregator de-duplicates by task_id and walks alphabetically). This
# file ships an empty `gaps: []` so the lstm_stack_bf16 composite is
# visible to the aggregator as "reported clean".
kernel_name: "dorado_fast_lstm_stack_bf16"
target: "aie2p"
compiler: "peano"
build_date: "2026-04-25T05:15:00+00:00"
host: "matteius-ProArt-P16-H7606WI"
gaps: []
